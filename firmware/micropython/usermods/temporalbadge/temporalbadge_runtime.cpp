// Implementations live in src/ReplayMicropythonAPI.cpp where they have access
// to the global C++ objects (badgeDisplay, inputs, badgeMatrix).
// This file is intentionally empty -- kept so the directory structure is clear.
